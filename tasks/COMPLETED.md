# COMPLETED — завершённые задачи

- [DONE] **PROJECT_SPEC.md v0.2** — APPROVED (AD-17..AD-28, обновлено M6: video/audio исполнимы).
- [DONE] **docs/00..18_*.md** — APPROVED (производная документация).
- [DONE] **AI engineering documentation layer** — AGENTS.md + engineering/* + tasks/* созданы, APPROVED.
- [DONE] **workflows/video_generate** — на этапе M4 DECLARED_ONLY манифест для media-agnostic теста; в M6 стал исполнимым (DECLARED_ONLY снят, добавлен `workflow.json`).
- [DONE] **M1 — Runtime + Client** — ComfyClient + RuntimeInfo; 8 тестов на живом ComfyUI.
- [DONE] **M2 — Asset Layer** — media-agnostic Asset + AssetStore; JSONL persistence + lineage + security; 10 passed, 1 skipped.
- [DONE] **M3 — Capability + Workflow Registry** — CapabilityRegistry + WorkflowRegistry; 25 тестов; DECLARED_ONLY/UNKNOWN/ASSIGN корректно.
- [DONE] **M4 — Execution / Verification** — WorkflowEngine + ComfyUIProvider + WebSocket + Job + Verifier; txt2img E2E РЕАЛЬНО прошёл на локальном ComfyUI (~91s). Media-agnostic, без Agent/LLM/Model Registry.
- [DONE] **M5 — Provider + Model Registry + Remote Execution** — C-fixes (ws/wss, /history recovery, COMFY_URL); Model Registry per-backend (app/registry/model.py); Provider boundary preserved; remote E2E DoD test РЕАЛЬНО прошёл на Colab ComfyUI через Cloudflare Tunnel (Tesla T4). AD-29 доказан физически: local ComfyUI SUCCESS + Colab ComfyUI SUCCESS при одном execution path.
- [DONE] **M6 — Real Video E2E** — `video_generate` исполнимый workflow; реальный video-E2E доказан на remote Colab (Tesla T4) через Cloudflare Tunnel → локальный Windows AssetStore; `Asset(type=video)`, `Verifier(kind=video)`. `audio_generate` реализован как исполнимый workflow, но real E2E blocked by Sonilo 401 (deferred).
- [DONE] **Agent orchestration layer** — `Agent` + `HeuristicPlanner` + `LLMPlanner` (OpenRouter) + multi-backend catalog (`BackendCatalog`) + asset inputs; media-agnostic, поверх M1–M5. (Соответствует roadmap-M8, реализован раньше M7.)
- [DONE] **M6.5 — Image Input / img2img** — `image.edit` исполнимый workflow (`workflows/img2img`): реальный ComfyUI graph (LoadImage → VAEEncode → KSampler(denoise) → VAEDecode → SaveImage). Закрыт gap AD-23: `asset_inputs.image` (node/field/kind) декларативно связывает Asset → ComfyUI input; video Asset → INPUT_INCOMPATIBLE. Media-agnostic (тот же WorkflowEngine/Job/Verifier/Asset, без ImageEngine/ImageAsset). Offline-тесты: манифест, совместимость image/video, декларативная привязка, lineage. Real-E2E на живом ComfyUI — при наличии backend (`COMFY_REMOTE_URL`), иначе skip (без mock).
- [DONE] **M7 — Conversation Context** — `app/conversation.py`: `ConversationContext` (media-agnostic, только id/строки) + `ConversationAgent(Agent)` (session-scoped `sessions`). Multi-turn chain доказан офлайн: generate → Asset A → image.edit на `active_asset`=A → Asset B, `lineage(B)==[B,A]`, `active_asset`=B. Session isolation доказана (разные session не видят чужие active_asset/assets). Explicit asset override активного — работает. Ошибка НЕ заменяет active_asset (запись в `unresolved`). `Agent.resolve_asset_inputs` расширен (обратно совместимо): приоритет explicit > active_asset > reference; тип active сопоставляется с `role.kind` (AD-23, без транскодинга). Существует независимо от LLM. `tests/test_conversation_m7.py` (8): 7 passed, 1 skip (remote E2E).

- [DONE] **M9 — UI (чат + preview + SSE)** — `app/ui.py`: минимальный веб-сервер (stdlib `http.server`, без новых зависимостей) поверх `ConversationAgent`. `POST /turn` запускает `ConversationAgent.turn` в фоновом потоке; `GET /events` стримит SSE (start → status RUNNING → result|error с превью `active_asset`); `GET /asset/<id>` отдаёт байты ассета (preview по типу); `GET /api/session` — контекст session. Честный progress = переходы состояния Job (без fake-процентов). Session isolation сохранён. `tests/test_ui_m9.py` (5): индекс, turn→asset+preview, SSE start/result, session isolation, asset 404.

- [NOTE] **image.upscale** — реализован: `workflows/upscale` (LoadImage → ImageScale (lanczos) → SaveImage). Capability `image.upscale`, asset_inputs.image (kind=image), без checkpoint/custom nodes. AD-23: image совместим, video INPUT_INCOMPATIBLE. 5 тестов (1 skip remote E2E).
- [DONE] **plan_local_e2e_validation (2026-09-08)** — PHASE 0–3 на реальном локальном Comfy Desktop 0.34.5 (:8188, CPU). READY: image.generate, image.edit, image.inpaint (создано), text.generate (после исправления workflow), video.generate, video.image_to_video; LIMITED: image.upscale (resize ≠ model upscale); BLOCKED: audio.generate (нет Comfy.org ключа). Исправлены дефекты: `_build_multi_asset_input` (BatchImagesNode Autogrow dotted), text_generate workflow (SaveText.format, prompts.text_1, api_key). +18 unit·E2E тестов (test_text_generate 8, test_multi_asset +2, e2e_validation inpaint). Сводка и per-capability отчёт: `docs/PLAN_LOCAL_E2E_VALIDATION.md`.
- [DONE] **M11 Prompt Builder** — ✅ M11.3-M11.6 FULLY IMPLEMENTED (2026-09-01). `app/prompt/{__init__,builder,heuristic,templates,llm,composite}.py` — HeuristicPromptBuilder (offline) + LLMPromptBuilder (online) + CompositePromptBuilder (fallback orchestration) + Planner integration. `app/agent.py` — prompt_builder DI, enhancement в generate(). `app/conversation.py` — enhancement в turn(). Тесты: `tests/test_prompt_builder_m11.py` (8) + `tests/test_prompt_builder_llm_m11.py` (11) + `tests/test_prompt_builder_composite_m11.py` (12) + `tests/test_prompt_builder_integration_m11.py` (13). AD-30/31/32 соблюдены.
- [DONE] **M25 — Experience Foundation** — ✅ IMPLEMENTED & FROZEN (2026-09-11). `app/engine/experience.py`: `ChainExperience` + `SequenceExperience` (computed view, M25_ARCHITECTURE_REVIEW §3.4 — НЕ отдельная persistence), `build_chain_experience()`, `build_sequence_experience()`. M25.1 chain_id tracking (`ExecutionRecord.chain_id`, `get_by_chain()`). M25.2 `video.image_to_video` manifest fix (`required_custom_nodes: ["CreateVideo","SaveVideo"]`, `model_requirements`). M25.3 `SemanticVerifier.verify_temporal_consistency()` подключён в production pipeline (`app/conversation.py:595`, B1 closed). M25.4 `build_sequence_experience()` встроен в experience flow (`app/conversation.py:643`, B2 closed). Tests: `test_temporal_verification` (11) + `test_video_image_to_video` (15) + `test_m25_b1_b2_integration` (12) + ранее `test_experience` (13) + `test_chain_tracking` (22) + `test_multi_asset` (11). 97 M25-related pass, 0 new failures. Acceptance: `docs/M25_FORENSIC_ACCEPTANCE_AUDIT.md` → `M25 READY FOR ACCEPTANCE`.
- [ACCEPTED] **M26.1 — Experience Analytics** — ✅ IMPLEMENTED & ACCEPTED (2026-09-11). `app/engine/experience.py`: `ExperienceAnalytics` (read-only aggregation над `ExperienceStore`): `temporal_stats()`, `preferred_params()` (ranking по непрерывному temporal score, `EXPERIENCE_MIN_SAMPLES_FOR_PREFERENCE=2` — переиспользует конвенцию `count>=2`, НЕ magic `0.7`). `ExperienceHint` dataclass (signal). Нет новой persistence, нет mutation исходных Experience. Tests: `tests/test_m26_experience_analytics.py` (9).
- [ACCEPTED] **M26.2 — Experience → AdaptivePlanner** — ✅ IMPLEMENTED & ACCEPTED (2026-09-11). `AdaptivePlanner(experience_store=...)` применяет experience-preference как soft default (ranking, НЕ prohibition). `PlanContext` M9.1 и `ExecutionRecord` НЕ изменены. Нет magic threshold. Tests: `tests/test_m26_adaptive_planner.py` (6).
- [ACCEPTED] **M26.4 — Experience → Composer suggestions** — ✅ IMPLEMENTED & ACCEPTED (2026-09-11). `Composer.compose(experience_hint=...)` добавляет computed suggestion (не меняет chain/alternatives, не auto-policy). `Experience → signal → suggestion`. Tests: `tests/test_m26_composer_suggestion.py` (4).
- [REDEFINED] **M26.3 — Video Editor Integration Boundary** — 🔄 REDEFINED / DEFERRED. Исходная постановка «frame extraction → temporal score → SUCCESS/FAILED» переопределена: video-specific analysis принадлежит будущему отдельному проекту **Video Editor / Media Project**. Agent НЕ содержит video-processing слоя. Variant A (advisory) старой формулировки НЕ реализуется сейчас; Variant B / **AD-44 SUPERSEDED / NOT APPROVED**. Обычный путь `Agent → ComfyUI → asset` не зависит от Video Editor. Не реализовано как video-processing внутри Agent (cv2/ffmpeg/frame-extraction/новый verifier НЕ добавлены). Детали: `docs/M26.3_FORENSIC_DESIGN.md`.
- [CLOSED] **D12 — FeedbackStore → AdaptivePlanner** — wiring уже существует (М24/М19), подтверждён `test_m24_1_production_wiring.py`. Не gap, не изменять.
- [SUPERSEDED] **AD-44 — Output-Video Temporal Quality Gate** — DRAFT / SUPERSEDED / NOT APPROVED. Исходная post-hoc FAILED semantics признана принадлежащей downstream media boundary (Video Editor). Не принимать, не реализовывать.
